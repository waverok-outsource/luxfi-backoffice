const baseUrl = "/v1";

const CustomerRoute = {
  customers: `${baseUrl}/customers`,
  analytics: `${baseUrl}/analytics/customers`,
  kycTiers: `${baseUrl}/kyc-tiers`,
};

export default CustomerRoute;
