const baseUrl = "/v1";

const MarketplaceRoute = {
  analytics: `${baseUrl}/analytics/marketplace`,
  assetMarket: `${baseUrl}/asset-market`,
  orders: `${baseUrl}/orders`,
};

export default MarketplaceRoute;
